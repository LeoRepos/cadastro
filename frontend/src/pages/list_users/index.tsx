import * as styled from "./style";

export const ListUsers = () => {
    return (        
        <styled.ListUserContianer>
            
            <h1>LISTAR USUÁRIOS</h1>

        </styled.ListUserContianer>        
    );
}